---
interface Props {
  n: number,
  mobile?: boolean
}

const { n, mobile = false } = Astro.props;
---
{new Array(n).fill('_').map((_, i) => {
  if (mobile) {
    return <slot class='max-md:!overflow-y-hidden' />
  }
  else {
    return <slot />
  }
})}